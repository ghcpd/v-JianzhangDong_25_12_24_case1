{
  "problems": [
    {
      "id": 1,
      "file": "README.md",
      "line_numbers": [12,13,14],
      "original": "python -m venv venv\nvenv\\Scripts\\activate",
      "updated": "python -m venv .venv\n.\\venv\\Scripts\\Activate.ps1  (PowerShell)\n.venv\\Scripts\\activate  (cmd.exe)",
      "fix_explanation": "Standardized the virtual environment name to `.venv` (project convention), added platform-specific activation instructions and aligned the README with the project's automation which creates `.venv`."
    },
    {
      "id": 2,
      "file": "README.md",
      "line_numbers": [23,24,25,28,30,31,32],
      "original": "copy .env.example .env\n\nEdit .env and set the following values:\nAPP_ENV=dev\nPORT=8000\nDEBUG=true",
      "updated": "The project reads configuration from `settings.yaml` and requires the environment variable `APP_MODE`.\nSet `APP_MODE=production` before running (PowerShell: `$env:APP_MODE = 'production'`, cmd: `set APP_MODE=production`).\nPort is defined in `settings.yaml` (default: 9123).",
      "fix_explanation": "Removed incorrect `.env` instructions and keys (`APP_ENV`, `PORT`, `DEBUG`) and replaced them with the actual configuration mechanism (`settings.yaml`) and the required `APP_MODE` environment variable used by `config.load_config()` in the code."
    },
    {
      "id": 3,
      "file": "README.md",
      "line_numbers": [35,36,37,41,42],
      "original": "python main.py\n\nOpen your browser and visit:\nhttp://localhost:8000",
      "updated": "Run with `python app.py`. The application prints `Service started successfully` and the configured port (default 9123). Visit the configured port (default: http://localhost:9123).",
      "fix_explanation": "Corrected the application entrypoint from `main.py` to `app.py` and updated the default port to match `settings.yaml` (9123)."
    },
    {
      "id": 4,
      "file": "README.md",
      "line_numbers": [17,18,19,20,21],
      "original": "pip install -r requirements.txt",
      "updated": "pip install -r requirements.txt\n# install pytest for running the test-suite: pip install pytest",
      "fix_explanation": "Clarified that `pytest` is required to run the test suite and included it in the testing instructions (the repository's tests are executed by pytest)."
    }
  ]
}
